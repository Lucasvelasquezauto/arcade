# M2.2 — Integración de `m2-teclado` en `main`

**Estado:** Encargo completado de punta a punta. Merge limpio, `pnpm verify` y `pnpm build`
pasaron, push a `main` aceptado, y la rama/worktree de `m2-teclado` quedaron eliminados. No
hubo ningún conflicto ni fallo que reportar.

---

## 1. Tarea (a) — Commit de docs en `main`

`git status` antes de tocar nada mostró exactamente lo esperado y nada más:

```
Changes not staged for commit:
	modified:   docs/execution-plan.md
Untracked files:
	docs/estado.md
```

Sin nada más colado en el working tree. Comiteé ambos juntos:

```
git add docs/estado.md docs/execution-plan.md
git commit -m "Traspaso M1->M2: agrega docs/estado.md y actualiza execution-plan"
```

Resultado: commit `aa1149a` — `2 files changed, 97 insertions(+)`.

## 2. Tarea (b) — Integrar `m2-teclado` en `main`

### 2.1 Merge

Desde el checkout raíz (`main`, no el worktree):

```
git merge m2-teclado --no-ff -m "Integra m2-teclado: ..."
```

Resultado: **merge limpio, sin conflictos** (`Merge made by the 'ort' strategy.`). Commit de
merge `875c692`. Archivos tocados por el merge:

```
apps/arcade/src/game-session.ts            |  91 ++++++++-----
docs/handoff/2.1-teclado.md                | 209 +++++++++++++++++++++++++++++
packages/shell/package.json                |   1 +
packages/shell/src/cabinet/Cabinet.ts      |  64 +++++++--
packages/shell/src/cabinet/layout.ts       |  22 +++
packages/shell/src/controls/Button.ts      |  58 +++++++-
packages/shell/src/controls/Stick.ts       |  90 +++++++++++--
packages/shell/src/controls/keyboard.ts    |  57 ++++++++
packages/shell/src/controls/touch.ts       |  71 +++++-----
packages/shell/src/index.ts                |  10 ++
packages/shell/src/platform/pointer.ts     |  22 +++
packages/shell/src/theme/tokens.ts         |  12 ++
packages/shell/src/types.ts                |  27 ++--
packages/shell/test/cabinet-layout.test.ts |  34 +++++
packages/shell/test/keyboard.test.ts       |  62 +++++++++
packages/shell/test/touch.test.ts          |  37 ++---
pnpm-lock.yaml                             |   3 +
17 files changed, 746 insertions(+), 124 deletions(-)
```

Todo dentro de `packages/shell`, `apps/arcade`, `docs/handoff/` y `pnpm-lock.yaml` — nada en
`packages/core` ni `packages/contracts`. `STICK_DEAD_ZONE_RATIO` no lo toqué; quedó tal cual
salió del merge automático (0.25 en `packages/shell/src/controls/touch.ts`, el SUPUESTO
señalado en `docs/handoff/2.1-teclado.md` §4.5, sin resolver).

### 2.2 Verificación

`pnpm verify` — pasó limpio: tipos, ESLint constitucional, dependency-cruiser (88 módulos,
192 dependencias, sin violaciones), 29 archivos de test / 158 tests pasados, y la prueba
negativa (`verify:rules`) mordió como debía.

`pnpm build` — pasó limpio: `vite build` compiló 134 módulos y generó el PWA
(`dist/sw.js`, `dist/workbox-*.js`) sin errores.

Ningún fallo nuevo respecto a lo que pasaba en las dos ramas por separado antes del merge.

### 2.3 Push

```
git push origin main
```

Resultado: `f6337fb..875c692  main -> main` — **no rechazado**.

### 2.4 Limpieza de rama y worktree

```
git branch --merged main
```

mostró `m2-teclado` en la lista (junto con `main`), confirmando que quedó integrada.

```
git worktree remove C:\Users\lucas\Claude\Projects\Arcade\.worktrees\m2-teclado
```

— salió sin error (exit 0), `git worktree list` ya no la muestra.

```
git branch -d m2-teclado
```

— `Deleted branch m2-teclado (was 9882aba).` Sin problemas.

## 3. Confirmación explícita: `packages/core` y `packages/contracts` intactos

```
git diff --stat f6337fb..875c692 -- packages/core packages/contracts
```

Salida vacía — ningún archivo de esas dos carpetas cambió entre el estado previo a esta
sesión (`f6337fb`) y el estado final tras merge (`875c692`), que incluye tanto el commit de
docs como el merge.

`git status` final: `working tree clean`, `main` al día con `origin/main`.

## 4. SUPUESTO / notas

- No hubo ninguna decisión ambigua que requiriera un `SUPUESTO` de mi parte en este encargo:
  fue mecánico contra un plan ya decidido y todo salió según lo esperado (merge limpio,
  verify/build limpios, push aceptado, limpieza sin sorpresas).
- No se disparó ningún deploy manual — el push a `main` ya lo activa solo en Vercel, según
  la instrucción explícita de no hacer nada adicional.

## 5. Historial resultante en `main`

```
875c692 Integra m2-teclado: modo teclado en PC, ampliacion del mueble a la ventana, dos correcciones de contrato
aa1149a Traspaso M1->M2: agrega docs/estado.md y actualiza execution-plan
9882aba M2.1: modo teclado, ampliación de ventana en PC, y dos correcciones de contrato
f6337fb Handoff M1.14: corrige VITE_SUPABASE_URL en Vercel, redeploy, verifica escritura real y limpia datos de prueba
```
