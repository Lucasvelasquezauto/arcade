/**
 * @arcade/game-test-pattern — andamiaje vacío (M1.1). Sin lógica todavía: el
 * `GameModule` de prueba llega en M1.6 (docs/execution-plan.md,
 * docs/specs/walking-skeleton.md §3).
 */
export {};
